fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
requests==2.32.3

# ML deps
# Keep numpy < 2 for widest binary compatibility
numpy==1.26.4
# Some SAM-Audio deps require protobuf < 3.20
protobuf>=3.9.2,<3.20

# transformers 4.57.x requires huggingface-hub < 1.0
transformers==4.57.3
huggingface-hub>=0.34.0,<1.0
# sam-audio is installed separately in Dockerfile with --no-deps to prevent PyTorch override
# git+https://github.com/facebookresearch/sam-audio.git

# sam-audio runtime dependencies (without torch/torchaudio which we control)
einops
librosa
soundfile
pydub
torchdiffeq
audiobox_aesthetics


